{
  "name": "site",
  "private": true,
  "description": "Dave Martinez's Website",
  "version": "0.1.0",
  "scripts": {
    "deploy": "gatsby build && gh-pages -d public --branch master",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  },
  "dependencies": {
    "gatsby": "^2.23.12",
    "gh-pages": "^3.1.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dkm-coder/site"
  },
  "bugs": {
    "url": "https://github.com/dkm-coder/site/issues"
  }
}
